services:
  rabbitmq:
    image: rabbitmq:3-management
    ports:
      - "5678:5672"     # AMQP
      - "15678:15672"   # Management UI (guest / guest)
    environment:
      RABBITMQ_DEFAULT_USER: guest
      RABBITMQ_DEFAULT_PASS: guest
